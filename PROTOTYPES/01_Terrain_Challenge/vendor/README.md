# Vendored Three.js runtime

`three.module.js`, `three.core.js`, and `OrbitControls.js` are a pinned Three.js r179 browser distribution copied byte-for-byte from the repository-local dependency set. They keep this isolated prototype offline and remove any install or CDN requirement.

The upstream Three.js MIT licence is preserved in `THREE-LICENSE.txt`.
