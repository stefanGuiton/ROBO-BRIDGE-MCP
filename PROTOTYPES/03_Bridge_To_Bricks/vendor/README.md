# Vendored Three.js runtime

`three.module.js`, its `three.core.js` sibling, and `OrbitControls.js` are local browser assets copied byte-for-byte from the repository's existing `PROTOTYPES/01_Terrain_Challenge/vendor/` dependency set so this prototype runs without a network or npm install.

The upstream Three.js MIT licence is preserved in `THREE-LICENSE.txt`.
